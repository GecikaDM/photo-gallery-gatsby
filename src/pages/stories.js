import * as React from 'react';
import Layout from '../components/layout';

const Stories = () => {
    return (
        <Layout pageTitle="Stories">
            
        </Layout>
    )
}

export const Head = () => <title>Stories</title>

export default Stories;